<!DOCTYPE html>
<html oncontextmenu="return false" onselectstart="return false">
<head>
    <meta charset="UTF-8">
    <title>로그인</title>
    <style>
        td{
            padding: 12px;
        }
        input{
            font-size: 16px;
        }
        h1{
            font-family: HY동녘M;
            font-size: 72px;
            color: paleturquoise;
            text-shadow: 0 0 4px black;
        }
        .box{
            padding: 10px;
            border: 3px solid yellowgreen;
            padding-left: 18px;
            width: 270px;
        }
        label{
            font-family: Cooper Blk BT;
            font-size: 18px;
        }
        .button{
            border: none;
            background-color: white;
            padding: 7px;
            outline: none;
            color: lightslategrey;   
            font-weight: bold;  
            font-size: 27px;
            text-shadow: 0px 1px #ffffff, 1px 1px 0px #dad7d7;
        }
        .text{
            font-weight: bold;  
            font-size: 15px;
            text-decoration: none;
            color: black;
            padding: 5px;
            padding-right: 25px;
            padding-left: 25px;
            text-shadow: 0px 1px #ffffff, 1px 1px 0px #dad7d7;
        }
        .box1{
            width: 25%;
            height: 0px;
            background-color:rgb(208, 208, 208);
            border: none;
        }
    </style>
    <script language="javascript" src="https://ajax.aspnetcdn.com/ajax/jQuery/jquery-3.3.1.min.js"></script>
    <script>
        function check() {
            const getemail = RegExp(/^[A-Za-z0-9_\.\-]+@[A-Za-z0-9\-]+\.[A-Za-z0-9\-]+/);
            const getpw = RegExp(/^[a-zA-Z0-9]{4,12}$/);
        
            //이메일 공백 확인
            if($("#id").val() == ""){
                alert("이메일을 입력해주세요.");
                $("#id").focus();
                return false;
            }
                
            //이메일 유효성 검사
            else if(!getemail.test($("#id").val())){
                alert("이메일형식에 맞게 입력해주세요.")
                $("#id").val("");
                $("#id").focus();
                return false;
            }

            //비밀번호 공백 확인
            if($("#pw").val() == ""){
                alert("비밀번호를 입력해주세요.");
                $("#pw").focus();
                return false;
            }

            //비밀번호 유효성
            else if(!getpw.test($("#pw").val())) {
                alert("비밀번호는 4~12자의 영문 대소문자와 숫자로만 입력해주세요.");
                $("#pw").val("");
                $("#pw").focus();
                return false;
            }
        }
        
        if(<%= ERROR %> == 100){
            alert("등록되어있지 않은 회원이거나 아이디 또는 비밀번호가 틀렸습니다.");
        }

        if(<%= ERROR %> == 200){
            alert("등록되어있지 않은 회원이거나 입력하신 값이 없습니다.");
        }

        if(<%= SUCCESS %> == 100){
            alert("정상적으로 회원가입이 되었습니다.");
        }
        
    </script>
</head> 
<body>
<center>
    
    <form method="post" action="/login" onsubmit="return check()">
        <h1>로그인</h1>
        <table>
            <tr>
                <td><label>I&nbsp;D :</label></td>
                <td><input type="text" name="id" id="id" class="box" placeholder=" EMAIL"></td>
            </tr>
            <tr>
                <td><label>PW :</label></td>
                <td><input type="password" name="pw" id="pw" class="box" placeholder=" PW"></td>
            </tr>
        </table>
        <input type="submit" value="로그인" class="button"><br>
        <input type="text" class="box1" disabled><br><br>
        <a href="/idscan" class="text">ID 찾기</a>|<a href="/pwscan" class="text">PW 찾기</a>|<a href="/signup" class="text">회원가입</a>
    </form>
</center>
</body>
</html>